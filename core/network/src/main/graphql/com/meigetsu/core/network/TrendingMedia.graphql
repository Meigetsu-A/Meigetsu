query GetTrendingMedia($type: MediaType) {
  Page(page: 1, perPage: 20) {
    media(type: $type, sort: [TRENDING_DESC]) {
      id
      title {
        romaji
        english
      }
      coverImage {
        extraLarge
      }
      averageScore
      status
      format
    }
  }
}
